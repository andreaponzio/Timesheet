<form id="form" action="/activitygroup/{{data.id}}" method="post">
   <input type="text" id="_method" name="_method" value="post" hidden>

   <!-- Customer data -->
   <div class="d-flex d-flex-row">
      <div class="css-flex-8">
         <label for="id" class="form-label">Gruppo attività</label>
         <input type="number" class="form-control" id="id" value="{{data.id}}" readonly>
      </div>
      <div class="flex-grow-1 ms-2">
         <label for="description" class="form-label">Descrizione</label>
         <input type="text" class="form-control" id="description" name="description" value="{{data.description}}"
                maxlength="40" required>
      </div>
   </div>

   <!-- Save change & Delete customer -->
   <div class="d-flex d-inline-row justify-content-center mt-2">
      <button type="submit" class="btn btn-success me-1">
         <i class="bi bi-floppy"></i>
      </button>
      {{#unless data.wbs}}
         <button type="button" class="btn btn-danger me-1" onclick="deleteObject({{data.id}});">
            <i class="bi bi-trash"></i>
         </button>
      {{/unless}}
      <a href="/activitygroup" type="button" class="d-inline-flex btn btn-secondary">
         <i class="bi bi-x-square"></i>
      </a>
   </div>

   <!-- List of Activities -->
   <h6>Attività</h6>
   <ul class="list-group p-2 css-font-size-95">
      {{#each data.activity}}
         <a href="/activity/{{id}}" class="list-group-item list-group-item-action css-listgroup-item">
            <div class="d-flex flex-row align-items-center">
               <div class="css-flex-95">
                  <span>{{description}}</span><br>
                  <span class="css-summarize fst-italic">{{dwbs}} | </span>
                  <span class="css-summarize fst-italic">{{internal_ref}} | {{external_ref}}</span>
               </div>
               <div class="border-start css-flex-5 text-center">
                  {{hour}}<br><span class="css-summarize-number">Totale</span>
               </div>
               <div class="border-start css-flex-5 text-center">
                  {{total}}<br><span class="css-summarize-number">Consumate</span>
               </div>
               <div class="border-start css-flex-5 text-center">
                  {{request}}<br><span class="css-summarize-number">Trasporti</span>
               </div>
            </div>
         </a>
      {{/each}}
   </ul>
   {{> YesOrNo}}
   {{> Error}}
</form>

<script>
   function modalSubmit() {
      document.querySelector("#form").submit();
   }
   function deleteObject(id) {
      document.querySelector("#_method").value = "delete";
      let modal = new bootstrap.Modal("#YesOrNo");
      modal.show();
   }
</script>