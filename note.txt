//-----------------------------
import CRequest from "./core/CRequest";
import * as zlib from "node:zlib";
let o: CRequest = new CRequest();
o.load(500933);
let zz: any = zlib.gzipSync(Buffer.from(JSON.stringify(o.data), 'utf8'));
console.log(zz);
let yy:any = JSON.parse(zlib.gunzipSync(zz).toString('utf8'));
console.log(yy);